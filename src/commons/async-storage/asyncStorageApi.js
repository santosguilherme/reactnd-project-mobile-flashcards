import {AsyncStorage} from 'react-native';


export default class AsyncStorageApi {
    constructor(key) {
        this.STORAGE_KEY = key;
    }

    get(id) {
    }

    query() {
    }

    save(id, body) {
    }

    remove(id) {
        // TODO: implementar
    }
}